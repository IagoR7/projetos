import random

#nomes = ['natan','haniel','breno','guilherme','rafael']
#numeros = [12,88,45,50,33]
#max - calcula o maior valor em um vetor
#min - calcula o menor valor em um vetor
#sum - soma os valores de um vetor
#append() - Insere um valor na ultima posição do vetor
#clear()	Removes all the elements from the list
#copy()	Returns a copy of the list
#count() - conta quantos valores tem no vetor
#extend()-Add the elements of a list (or any iterable), to the end of the current list
#index()-	Returns the index of the first element with the specified value
#insert() - Insere um valor na posição desejada no vetor
#pop()-	apaga o ultimo vetor
#remove()-Apaga o vetor pelo valor
#reverse()- Reverter a ordem da lista
#sort() - ordena o vetor
#len - conta quantos valores tem no vetor



#maior = max(numeros)
#menor = min(numeros)
#soma = sum(numeros)
#sortudo = random.choice(nomes)
#print(sortudo)

numeros = [12,88,45,50,33]

for x in range (5):
    print(f'A multiplicação por 20 é {numeros[x]*20}')
    



